# Core web app
Flask>=2.2
Flask-Cors>=4.0

# Streamlit UI
streamlit>=1.36

# Async HTTP client
aiohttp>=3.9

# HTML parsing
beautifulsoup4>=4.12

# Report generation (optional, used for PDF export in app.py)
reportlab>=4.0

# JS rendering for crawler (optional; enable with --js-render)
# After installing, run: python -m playwright install
playwright>=1.45
